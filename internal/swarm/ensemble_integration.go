//go:build ensemble_experimental
// +build ensemble_experimental

package swarm

// NOTE: Intentionally left blank to avoid an import cycle with the ensemble
// package. Use ensemble.NewEnsembleManager directly when needed.
